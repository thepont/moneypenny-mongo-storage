{
  "name": "auth-server",
  "version": "0.0.1",
  "description": "Authentication Server.",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "blueflag",
  "dependencies": {
  }
}
